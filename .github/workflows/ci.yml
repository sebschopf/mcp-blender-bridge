name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test-controller:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"
        
    - name: Install uv
      run: |
        curl -LsSf https://astral.sh/uv/install.sh | sh
        
    - name: Install dependencies
      run: |
        uv pip install --system -r controller/requirements.txt
        uv pip install --system -e controller/
        
    - name: Lint with Ruff
      run: |
        python -m ruff check controller/
        
    - name: Run Controller Tests
      env:
        GEMINI_API_KEY: "dummy_key_for_ci"
      run: |
        python -m pytest controller/
